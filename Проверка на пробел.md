
```
mov rdi, адрес_байта_с_значением_32  ; допустим, [rdi] = 0x20 (пробел)
test BYTE [rdi], -33                 ; эквивалентно test BYTE [rdi], 0xDF

; Побитовая операция:
; 00100000  (значение ASCII для пробела)
; 11011111  (значение -33 в дополнительном коде)
; ------------------------
; 00000000  (результат операции AND)

; Флаг ZF будет установлен, что можно проверить:
jz метка_пробела                     ; переходим, если [rdi] был пробелом
```


[[NASM]]